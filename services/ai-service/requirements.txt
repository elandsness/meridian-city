fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
python-json-logger==2.0.7

# LLM clients
openai==1.51.0
anthropic==0.34.2

# Observability — OpenLLMetry (Traceloop) owns the trace SDK and auto-instruments
# the OpenAI/Anthropic clients above, emitting OTel GenAI (gen_ai.*) spans. It
# exports OTLP/HTTP to the collector via OTEL_EXPORTER_OTLP_ENDPOINT. FastAPI
# request spans use the same tracer provider Traceloop configures.
# Versions are ranged so pip resolves a mutually-compatible OpenTelemetry set.
traceloop-sdk>=0.33,<1.0
opentelemetry-instrumentation-fastapi>=0.48b0
