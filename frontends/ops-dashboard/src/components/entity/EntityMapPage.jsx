// Generic entity map page — renders entities on a map.
// Currently a placeholder; will be implemented as part of the entity engine reskin.

export default function EntityMapPage({ entityType, config }) {
  return (
    <div className="rounded-xl border border-slate-200 bg-white p-6 text-center text-sm text-slate-400">
      Entity map for <code>{entityType}</code> — coming soon
    </div>
  )
}
